# Base URL of the backend API.
# Leave unset to auto-detect (assumes backend on port 5000 of the same host).
VITE_API_BASE=http://3.110.177.238:5000

# Environment label shown in the sidebar: "development" or "production"
VITE_APP_ENV=development
